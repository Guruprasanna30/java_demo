name: CI for demo app

on: 
  push:

jobs:
  CI:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: List the files
        run: ls -ltr

      - name: Secret
        run: echo ${{ secrets.MYSECRET }}

      - name: Echo branch name
        run: echo "Current branch is $(echo ${{ github.ref }} | awk -F '/' '{print $NF}')"
